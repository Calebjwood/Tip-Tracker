<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{job}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span for="img" aria-label="money">💲</span>
    <span class="dollar-amount">{{calculated tips}}</span>
    needed
  </p>
  <p>
    <button class="btn btn-primary">Tips</button>
  </p>
</div>
